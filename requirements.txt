# --- Core UI Dependencies (界面运行核心依赖) ---
# PyQt5 图形界面框架
PyQt5>=5.15.9

# PyQtWebEngine 用于加载 Cesium 地球 (main.py 中使用了 QWebEngineView)
PyQtWebEngine>=5.15.6

# pyqtgraph 用于绘制波形图表 (charts.py 中使用)
pyqtgraph>=0.13.3

# numpy 用于生成图表测试数据及后续科学计算
numpy>=1.24.0
scipy>=1.10.0

#其余必备依赖
xgboost>=2.1.4
lightgbm>=4.5.0
scikit-learn>=1.4.0
gdal>=3.6.2
openpyxl>=3.1.5
pandas>=2.2.0
matplotlib>=3.8.0

# --- Potential GIS Dependencies (后续开发建议依赖) ---
# 既然你的项目涉及 DEM (.tif) 和 Shapefile (.shp) 处理
# 建议加上这些库，方便客户后续填补 TODO 逻辑
# (如果客户只需要运行界面，可以将下面几行注释掉)

# rasterio>=1.3.0    # 用于读取处理 DEM/TIF 栅格数据
# geopandas>=0.13.0  # 用于处理 Shapefile 矢量数据
# shapely>=2.0.0     # 用于几何计算
#建议采用python 3.11版本
